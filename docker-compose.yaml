version: '3'

services:
  app:
    ports:
      - "10004:8082"
    image: zoe:dev
    environment:
      type: "web"
    deploy:
      replicas: 3
    volumes:
      - ./static:/home/zoe/static
      - ./config/config.yaml-docker:/home/zoe/config.yml
  cron:
    image: zoe:dev
    environment:
      type: "cron"
    volumes:
      - ./static:/home/zoe/static
      - ./config/config.yaml-docker:/home/zoe/config.yml